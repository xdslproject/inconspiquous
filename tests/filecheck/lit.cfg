import lit.formats
import os

config.name = "inconspiquous"
config.test_format = lit.formats.ShTest()
config.suffixes = ['.mlir', '.py']

config.substitutions.append(('QUOPT_ROUNDTRIP', "uv run quopt %s --print-op-generic --split-input-file | uv run quopt --split-input-file | uv run filecheck %s"))
config.substitutions.append(("QUOPT_GENERIC_ROUNDTRIP", "uv run quopt %s --print-op-generic --split-input-file | uv run filecheck %s --check-prefix=CHECK-GENERIC"))
